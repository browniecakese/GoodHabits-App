<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>GoodHabits App</title>
  <style>
      body {
        background-color: #ffe6ea; /* baby pastel pink */
        min-height: 100vh;
      }
      .welcome-box {
        background-color: white;
        border-radius: 16px;
        padding: 40px;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
        margin-top: 80px;
        max-width: 600px;
        width: 100%;
      }
      .btn-custom {
        background-color: #ff8fb1;
        border: none;
        color: white;
      }
      .btn-custom:hover {
        background-color: #ff6491;
      }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">GoodHabits App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <% if (!user) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Register</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li> 
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Logout</a>
                </li> 
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
</body>

<div class="container">
    <p style="text-align: center;">
        <% if (user) {%>
          <% if (user.role === "admin") { %>
            <br>
            <h1><p>Welcome, <b><%= user.username %> (<%= user.role %>)</b></p></h1>
             Go to <a href="/habitadmin">habitadmin</a>
          <% } else { %>
            <br>
            <h1><p>Welcome, <b><%= user.username %> (<%= user.role %>)</b></p></h1>
             Go to <a href="/habitlist">habitlist!</a>
            <% } %>
        <% } else { %>
        <br>
        <h1>Welcome to <b> GoodHabit App!</b></h1>
        <br>
        <a href = "/login" class="btn btn-primary ms-2" >Get started</a><br><br>
        <img src = "https://retirementorship.com/wp-content/uploads/2022/08/Build-Good-Habits.jpg" alt="good_habits" width=200px height=200px>
        <% } %>
    </p> 
</div>
