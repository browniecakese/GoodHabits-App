<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>GoodHabits App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">GoodHabits App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <% if (user && user.role === "admin") { %>
                <li class="nav-item">
                  <a class="nav-link" href="/habitlist">Habit List</a>
                </li> 
                <li class="nav-item">
                  <a class="nav-link" href="/addhabit">Add new habit</a>
                </li> 
            <% } %>
            </ul>
          </div>
        </div>
    </nav>

    <div class="border rounded p-3 bg-light text-center">
        <p>Welcome, <%= user.username %> (<%= user.role %>)</p>
        <br>
        <div class="text-center"><h2>Habit Details</h2></div>
        <br>
        <% if (habit) { %> <!-- Check if Habit data exists -->
            <div>
                <p><b>Habit Name:</b><br> <%= habit.name %></p>
                <p><b>Habit Image:</b><br> <img src = "../images/<%= habit.image %>" width="20%"></p>
                <p><b>Habit Type:</b><br> <%= habit.type %></p>
                <p><b>Habit Date:</b><br> <%= habit.date %></p>
                <p><b>Habit Description:</b><br> <%= habit.description %></p>
                <p><b>Habit Feeling:</b><br> <%= habit.feelings %></p>
            </div>
        <% } else { %>
            <p>No Habit found.</p>
        <% } %>
        <% if (user.role === "admin") { %>
          <a href ="/habitadmin">Back</a>
        <% } else { %>
          <a href ="/habitlist">Back</a>
        <% } %>
 

    </div>
    
</body>
</html>
